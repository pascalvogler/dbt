 steps:
 # Build the container image
 - name: 'gcr.io/cloud-builders/docker'
   args: ['build', '-t', 'gcr.io/$PROJECT_ID/dbt-service-test:$COMMIT_SHA', '.']
 # Push the container image to Container Registry
 - name: 'gcr.io/cloud-builders/docker'
   args: ['push', 'gcr.io/$PROJECT_ID/dbt-service-test:$COMMIT_SHA']
 # Deploy container image to Cloud Run
 - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
   entrypoint: gcloud
   args:
   - 'run'
   - 'deploy'
   - 'dbt-service-test'
   - '--image'
   - 'gcr.io/$PROJECT_ID/dbt-service-test:$COMMIT_SHA'
   - '--region'
   - 'europe-west6'
   - '--max-instances'
   - '10'
   - '--concurrency'
   - '80'
   - '--memory'
   - '512Mi'
   - '--service-account'
   - 'dungeon-lord@key-hope-278006.iam.gserviceaccount.com'
   - '--timeout'
   - '2100s'
 images:
 - 'gcr.io/$PROJECT_ID/dbt-service-test:$COMMIT_SHA'